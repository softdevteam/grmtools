name: Test fixed serialisation format from builder.
yacckind: Original(YaccOriginalActionKind::NoAction)
serialisation_format: SerialisationFormat::FixedSizeInteger
grammar: |
    %start Expr
    %avoid_insert 'INT'
    %%
    Expr: Expr '+' Term
        | Term
        ;

    Term: Term '*' Factor
        | Factor
        ;

    Factor: '(' Expr ')'
          | 'INT'
          ;

lexer: |
    %%
    [0-9]+ "INT"
    \+ "+"
    \* "*"
    \( "("
    \) ")"
    [\t ]+ ;
